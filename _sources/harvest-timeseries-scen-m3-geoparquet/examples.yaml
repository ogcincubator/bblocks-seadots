examples:
  - title: Harvest time series scenario Scen M3 — GeoParquet header
    description: >
      GeoParquetHeader document generated directly from the full source
      GeoJSON (19,920 Point features) using the geojson-to-geoparquet skill:
      geometry_types, bbox and CRS are computed from the actual data, not
      declared placeholders. propertyUrls are resolved from
      harvest-timeseries-scen-m3-source's context.jsonld.
    snippets:
      - language: json
        ref: examples/harvest-timeseries-scen-m3-header.json
