- title: SeaDOTs Catalog Data
  description: >
    Minimal generic SeaDOTs catalog data record with CF metadata and STAC
    provenance extension conformance.
  snippets:
    - language: json
      ref: examples/catalog-data.json
- title: SeaDOTs Catalog Data Input
  description: >
    Catalog data record used as an execution input. The input role is represented
    by properties.role rather than by a separate building block.
  snippets:
    - language: json
      ref: examples/input-stac-item.json
- title: SeaDOTs Catalog Data Output
  description: >
    Catalog data record used as an execution output. The output role and
    derivation links are represented inside the shared catalog-data profile.
  snippets:
    - language: json
      ref: examples/output-stac-item.json
