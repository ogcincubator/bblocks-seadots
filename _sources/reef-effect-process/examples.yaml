- title: Utsira reef-biomass process description
  description: >
    OGC API Processes Part 1 process description for the deterministic
    reef-biomass calculation `utsira_reef_biomass.py`. Each input and
    output schema is a `bblocks://` reference to the corresponding
    SeaDOTs per-class building block. A matching Execute request body
    is supplied for documentation in `examples/utsira_reef_biomass_execute.json`;
    that document conforms to OGC API Processes `execute.yaml`, not to this
    bblock's schema, and is therefore not validated here.
  snippets:
    - language: json
      ref: examples/utsira_reef_biomass_process.json
