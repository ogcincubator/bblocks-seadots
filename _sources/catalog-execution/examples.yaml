- title: SeaDOTs Catalog Execution
  description: >
    Minimal generic example that references the application, workflow, input,
    and output catalog records without duplicating their descriptive metadata.
  snippets:
    - language: json
      ref: examples/execution-record.json
