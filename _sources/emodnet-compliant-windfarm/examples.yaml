examples:
  - title: EMODnet sample windfarm feature
    content: >
      A point-based example that mirrors the official EMODnet windfarms XSD
      field names and uses the same geometry-to-attribute mapping as the service.
    snippets:
      - language: json
        ref: examples/operational-offshore-wind-farm.json
