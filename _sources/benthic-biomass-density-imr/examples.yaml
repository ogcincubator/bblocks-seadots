- title: IMR benthic biomass baseline — ICES IVa fallback
  description: >
    Per-taxon benthic biomass density with explicit uncertainty, used as
    fallback D_{pre,i} when MAREANO has no taxon coverage at index i.
    Aggregated from MAREANO Marbunn catch-sample records; raw point features
    are kept in `benthic-biomass-observations-imr`.
  snippets:
    - language: json
      ref: examples/imr_ices_iva_fallback.json
