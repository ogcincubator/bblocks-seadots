- title: Utsira Nord 60 × 15 MW — submerged infrastructure layout
  description: >
    Per-unit submerged geometry for 60 × 15 MW semi-submersible floating-wind
    units; aggregate submerged area 109,500 m² used as `A_sub` in the
    Utsira reef-biomass experiment.
  snippets:
    - language: json
      ref: examples/utsira_nord_60x15mw.json
